# Worker WhatsApp clinicOS — variables
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
# Clave SECRET (service_role). El worker bypasea RLS pero SIEMPRE filtra por tenant_id del channel.
SUPABASE_SECRET_KEY=sb_secret_xxx
# Cada cuánto poll de mensajes salientes pendientes (ms)
OUTBOUND_POLL_MS=4000
